# sg-pydoll

Scrape Gateway provider for Pydoll's direct Chrome DevTools automation.

```bash
sgw extensions sg-pydoll
sgw url https://example.com --render-js --screenshot -p pydoll
```

Chrome or Chromium must be installed on the host. The adapter supports CSS
selector waits and full-page PNG screenshots.
